node_modules
dist
.DS_Store
*.local
.env
.env.*
!.env.example

# Local-only Cursor rules, compliance docs, and pre-commit checks (not for the repo)
.cursor/
# Private review patterns (never commit — also covered by .cursor/ above)
.cursor/PATTERN_CHECK.md
